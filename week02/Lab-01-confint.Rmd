---
title: "Confidence Interval and Hypothesis Testing"
author: "Yang Meng"
output: 
  xaringan::moon_reader:
    css: ["slide-style.css", "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"]
    lib_dir: libs
    seal: false
    nature:
      beforeInit: "cols_macro.js"
      ratio: 16:9
      highlightStyle: "pygments"
      highlightLines: true
      highlightLanguage: "r"

---

class: title-slide


```{r echo = FALSE, message = FALSE}
library(tidyverse)
library(ggplot2)
options(scipen = 999)
```


<br>
<br>
.right-panel[ 

# `r rmarkdown::metadata$title`
## `r rmarkdown::metadata$author`


]


---

## Schedule for today

- Confidence interval for the population mean
- One-sample t-test
- Two-sample t-test
- Correlation test
- $\chi^2$ test

---

## Load the Packages and Data

```{r, message=F, warning=F}
library(tidyverse)
df <- read.csv("../data/Alzheimer_data.csv")
```

---

## Simulated Data

```{r}
set.seed(0)
norm_size <- 1000
norm_random <- rnorm(n = norm_size, mean = 10, sd = 2)
summary(norm_random)
```

---

## Suppose that the population variance is unknown.

```{r}
t_test_result <- t.test(x = norm_random)
t_test_result$conf.int
```

---

## Adjust the confidence level

```{r}
t_test_result <- t.test(x = norm_random, conf.level = 0.9)
t_test_result$conf.int
```

---

## Hypothesis Testing

```{r}
summary(norm_random)
```

$H_0:\mu=10$ vs. $H_A:\mu\neq10$
```{r}
t.test(x = norm_random, mu = 10)$p.value
```

---

## Hypothesis Testing (One-sided)

$H_0:\mu=10$ vs. $H_A:\mu>10$
```{r}
t.test(x = norm_random, mu = 10, alternative = "greater")$p.value
```

$H_0:\mu=9$ vs. $H_A:\mu>9$

```{r}
t.test(x = norm_random, mu = 9, alternative = "greater")$p.value
```

---

## Practice 1: Confidence Interval

Try to get a 90 percent confidence interval for the population mean of age among AD subjects.

```{r}
summary(df$age)
```

---

## Practice 1: Confidence Interval

Try to get a 90 percent confidence interval for the population mean of age among AD subjects.

```{r}
t_test_age <- t.test(df$age, conf.level = 0.9)
t_test_age$conf.int
```

---

## Practice 2: One Sample t-test

Test whether the population mean of age is 65 or greater than 65 and get the p-value.

$H_0:\mu=65$ vs. $H_A:\mu>65$

```{r}
summary(df$age)
```

---

## Practice 2: One Sample t-test

Test whether the population mean of age is 65 or greater than 65 and get the p-value.

$H_0:\mu=65$ vs. $H_A:\mu>65$

```{r}
t_test_age <- t.test(df$age, mu = 65, alternative = "greater")
t_test_age$p.value
```

---

## Comparing Two Samples

Is the average age for the AD subjects different between male and female?

```{r, echo = F}
ggplot(data = df, 
       aes(x = factor(female), y = age)) + 
  geom_boxplot() + 
  labs(x = "female")
```


---

## Two Sample t-test

Is the average age for the AD subjects different between male and female?

$H_0:\mu_M=\mu_F$ vs. $H_A:\mu_M \neq \mu_F$ 

```{r}
t.test(age ~ female, data = df)$p.value
```

---

## Two Numerical Variables

Question: Are age and blood pressure (BPSYS) correlated?


```{r, echo = F}
ggplot(data = df, 
       aes(x = age, y = bpsys)) + 
  geom_point()
```

---

## Correlation Test

$H_0:$ They are correlated. vs. $H_A:$ They are NOT correlated.

```{r}
cor.test(df$age, df$bpsys)$p.value
```

$H_0:$ They are positively correlated. vs. $H_A:$ They are NOT positively correlated.

```{r}
cor.test(df$age, df$bpsys, alternative = "greater")$p.value
```


---

## Two Categorical Variables

Question: Are gender and disease status associated with each other?

```{r}
contingency_table <- table(df$female, df$diagnosis)
contingency_table
```

---

## Pearson's $\chi^2$ Test of Independence

$H_0:$ They are independent vs. $H_A:$ They are NOT independent.

```{r}
chisq.test(contingency_table)
```



---

## Summary

- "t.test()" for one/two-sample t-test
- "cor.test()" for correlation test
- "chisq.test()" for $\chi^2$ test
- Useful arguments: "mu", "conf.level", "alternative"
- ?t.test, ?cor.test and ?chisq.test for more information


